# desc "Explaining what the task does"
namespace :action_validator do
  task :install do
    # TODO:
    # - Create default initializer in config/initializers/action_validator.rb
    # - Create the default app/javascript/channels directory and files if they do not exist. Maybe this should just be
    # in the docs.
  end
end
